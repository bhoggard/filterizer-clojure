(ns filterizer.core
  (:require [reagent.core :as r]
            [reagent.dom :as rdom]))

;; Placeholder for future dropdown with search functionality
;; This will be used for:
;; - Venue's neighborhood selection
;; - Event's venue selection

(defn init []
  (println "ClojureScript initialized - ready for dropdown components"))
